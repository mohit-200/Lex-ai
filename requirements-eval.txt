# Evaluation-only dependencies (heavy — not needed for the API server)
# Install separately: pip install -r requirements-eval.txt
-r requirements.txt

sentence-transformers==3.0.1
ragas==0.1.10
datasets==2.20.0
